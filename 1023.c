#include <stdio.h>
 
int main() {
 
    int n, pes, cons, on = 1, cont = 1;
    int matriz[][];

    while(on == 1){
        scanf("%d", &n);
        if(n == 0){
            on = 0;
            break;
        }

        printf("Cidade# %d:\n", cont);
        for(int i = 1; i <= n; i++){
            scanf("%d %d", &pes, &cons);
            
        }
    }
 
    return 0;
}